# Run with `dart run ffigen --config ffigen.yaml`.
name: MilibrisReaderBindings
description: |
  Bindings for `src/milibris_reader.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/milibris_reader_bindings_generated.dart'
headers:
  entry-points:
    - 'src/milibris_reader.h'
  include-directives:
    - 'src/milibris_reader.h'
comments:
  style: any
  length: full
