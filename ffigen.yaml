# Run with `dart run ffigen --config ffigen.yaml`.
name: MilibrisFFIBindings
description: |
  Bindings for `src/milibris_ffi.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/milibris_ffi_bindings_generated.dart'
headers:
  entry-points:
    - 'src/milibris_ffi.h'
  include-directives:
    - 'src/milibris_ffi.h'
comments:
  style: any
  length: full
