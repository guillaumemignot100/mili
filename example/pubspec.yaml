name: milibris_ffi_example
description: 'Demonstrates how to use the milibris_ffi plugin.'
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  milibris_ffi:
    path: ..
  jni: ^0.15.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
